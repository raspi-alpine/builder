#!/bin/sh

# set username for i2c owner and node-red
NME="megapi"

# set cpu govenor
echo conservative > /sys/devices/system/cpu/cpufreq/policy0/scaling_governor

su -c "node-red-pi flows.json" - "$NME" &
